import { describe, it, expect } from 'vitest'

describe('useCheckoutStepTrackingInterceptor()', () => {
  // TODO: Remove this demo test
  it('demo test', () => {
    expect(true).toEqual(true)
  })
})
