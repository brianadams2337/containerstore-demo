config:
  target: https://nuxt3-demo-latest.storefront.scayle.cloud/de
  engines:
    playwright: {}
  processor: './testScripts.ts'
  phases:
    # injects {arrivalRate} users every second for {duration} seconds
    - name: constantArrival
      duration: 30
      arrivalRate: 2
