---
'@scayle/storefront-application-nuxt': minor
---

**\[UI\]** Simplified Tailwind configuration by embracing default `z-index` values, eliminating custom overrides.
Streamlined usage by removing unnecessary `z-index` values (`60` to `110`), ensuring cleaner and more consistent styling.
