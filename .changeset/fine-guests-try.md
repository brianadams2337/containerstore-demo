---
'@scayle/storefront-application-nuxt': patch
---

**\[Accessibility\]** Resolved an issue in `SFPriceRangeSlider.vue` where focusing and blurring the price input in the filter modal triggered an unintended filter update by removing the price update on `change` event.
