---
'@scayle/storefront-boilerplate-nuxt': minor
---

**\[UI\]** Skip links are only intended to be used for keyboard navigation and should not interfere with mouse interactions. This change ensures that skip links are only visible and intractable when focused. Previously, clicking near a skip link would open it, which is not the intended behavior.
