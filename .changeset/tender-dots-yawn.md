---
'@scayle/storefront-application-nuxt': minor
---

**\[UI\]** Added the `focus:shadow-none` utility class to the `main` tag within the `default` layout.
This change addresses an issue in Safari where a shadow unexpectedly appears on the `main` container after closing the filters slide-in modal.
