---
'@scayle/storefront-application-nuxt': patch
---

**\[Accessibility\]** Resolved the issue that the focus outline of `SFSliderArrowButton.vue` was cut off because of an adjacent arrow button in `SFProductGallery.vue`.
