---
'@scayle/storefront-application-nuxt': patch
---

**\[UI\]** Increased the bottom margin of the `SFDealBadge` container in the `SFProductCard` and `SFBasketCardImage` components from `bottom-2.5` to `bottom-6`.

This will prevent the badge from overlapping with `SFProductCardInBasketBadge` component.
