---
'@scayle/storefront-application-nuxt': minor
---

**\[CMS\]** Fixed an issue where the `<CMSText />` component for contentful could only handle usage within a paragraph. Now it can also handle standalone usage by checking the node type and resolving the content accordingly.
