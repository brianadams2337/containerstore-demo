---
'@scayle/storefront-boilerplate-nuxt': patch
---

**\[UI\]** Add focus trap to `/modules/ui/runtime/components/core/SFModal.vue` This ensures a `SFModal.vue` is still responsive when a `SFSlideIn.vue` is open at the same time.
