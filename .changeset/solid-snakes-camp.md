---
'@scayle/storefront-application-nuxt': patch
---

**\[Accessibility\]** Added `role="button"` to the search input trigger to resolve the accessibility issue "Non-active element in tab order" by ensuring it behaves like a keyboard-accessible interactive element.
