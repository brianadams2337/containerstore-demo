---
'@scayle/storefront-application-nuxt': minor
---

Avoid using absolute URLs in breadcrumbs generated by `useBreadcrumbs()`. Nuxt interprets absolute URLs as external links, which triggers a full page reload. This change ensures that navigation via breadcrumbs happens on the client side for a smoother experience.
