---
'@scayle/storefront-application-nuxt': minor
---

**\[Architecture\]** Refactored slider arrow controls by replacing `SFProductCardImageSliderButton` and custom `SFButton` implementations with the new `SFSliderArrowButton` component.
This change centralizes logic and styling for a more maintainable and consistent codebase.
